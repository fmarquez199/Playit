world %Factorial%:
  monster factorial(Power n) Power:
  "'Hola soy un comentario, ignorame'"
    Button:
      | n < 0 } unlock 0
      | n == 0 } unlock 1
      | n > 0 } unlock n * kill factorial(n-1)
    .~
  .~
.~
