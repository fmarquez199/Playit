world %Caso9%:
  monster fibonnaci(Power primero, Power segundo, Power limite) Contacto:
    drop primero
    drop segundo
    Power n = primero
    play:
      drop primero + segundo
      primero = segundo
      segundo = n + segundo
      --limite
    lock limite > 0
    .~
    .~
  drop ~Nada importa, esto no compilara, saludos~
  kill fibonnaci(1, 2, 5)
.~ 
