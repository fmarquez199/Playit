world %HolaMundo%:

    Button edad = 18

    Battle puedeConducir
    Button:
    | edad >= 18}
      puedeConducir = Win
    .~
    
    Button:
    | edad >= 18}
      puedeConducir = Win
    | edad >= 18}
      puedeConducir = Win
    .~
    
    Button:
    | edad >= 18}
      puedeConducir = Win
    | edad >= 18}
      puedeConducir = Win
    | notPressed}
      puedeConducir = Win
    .~      
.~

