monster n(Power n) Power:
  @Skill n @Comentando esto deberia compilar
  play:
    Battle n
    drop n  @ verificar si esta inicializada?
  lock Win
  .~
  unlock n
.~

world %n%:
    @Falla con verificacion de tipos, no falla porque son categorias diferentes
    Power n
    n = kill n(n)
    
    @Power n = kill n(n) @falla con el arg porque la decl de 'n' no ha sido insertada todavia y toma la func
.~
