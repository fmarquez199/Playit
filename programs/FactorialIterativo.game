world %FactorialIterativo%:
  Power numero = joystick ~Por favor dime un numero: ~
  Power factorial = 1
  play:
    factorial = factorial * numero
    numero--
  lock numero > 1
  .~

  @Runes fact = kill portalPowerToRunes(factorial)
  
  Button:
    | numero > 0 }
      drop ~El factorial del numero indicado es ~, kill portalPowerToRunes(factorial)
    | numero < 0 }
      drop ~El factorial de un numero negativo no esta definido, saludos.~
  .~
  drop ~Nos veremos pronto.~
.~
